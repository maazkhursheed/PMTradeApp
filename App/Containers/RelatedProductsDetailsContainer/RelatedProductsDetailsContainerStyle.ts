import { StyleSheet } from "react-native";
import colors from "~root/Themes/Colors";

export default StyleSheet.create({
  loadingView: { flex: 1 },
  flatListStyle: { flex: 1, backgroundColor: colors.white, paddingBottom: 80 },
});
